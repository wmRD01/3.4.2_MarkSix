import { Button, Label, Node, Sprite } from "cc";
import BaseComponent from "../../../Model/ComponentBase";

export default class DataFunctionMenu extends BaseComponent {

    buttonBlockBG: Button;
    contentButton: Node;
    contentBG: Node;
    buttonMusic: Button;
    buttonRecord: Button;
    buttonExchange: Button;
    buttonGameInfo: Button;
    buttonChangeNickname: Button;
    buttonBackHome: Button;
    labelChangeNickname: Label;
}